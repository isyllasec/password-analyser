[pytest]
; Force pytest a ajouter la racine du projet au chemin d'import Python,
; pour que "from data.keyboard_layout import ..." et "from core.entropy
; import ..." fonctionnent aussi bien en lancant les tests qu'en lancant
; l'app (streamlit run app.py depuis la racine).
pythonpath = .
testpaths = tests